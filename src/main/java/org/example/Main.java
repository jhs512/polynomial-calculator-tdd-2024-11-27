package org.example;

import java.util.Arrays;

// 목표 : int rs = Calc.run("((3 + 5) * 5 + -10) * 10 / 5");  tdd 로 구현하기
public class Main {
    public static void main(String[] args) {
        int[] arr = {1, 2, 3};

        System.out.print(Arrays.toString(arr));
    }
}